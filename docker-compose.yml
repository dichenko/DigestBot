version: "3.8"

services:
  bot:
    build: .
    container_name: digest_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./user_session.session:/app/user_session.session
